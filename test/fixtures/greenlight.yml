version: 1.1.0
plugins:
  foo:
    enabled: true
    exclude: node_modules
    settings:
      foo: "bar"

  bar:
    enabled: true
    exclude:
      - node_modules
    settings:
      foo: bar

  baz: true
